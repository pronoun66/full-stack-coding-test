export enum FilterField {
  DURATION = 'duration',
  STARTED_AT = 'startedAt'
}

export enum FilterTimeUnit {
  SECOND = 's',
  MINUTE = 'm',
  HOUR = 'h'
}

export enum FilterOperator {
  GTE = 'gte',
  GT = 'gt',
  EQ = 'eq',
  LTE = 'lte',
  LT = 'lt'
}